Import("*")

gyros_srcs = map(target_env.File, [ 'task.c', 'suspend.c', 'sleep.c',
                                    'mutex.c', 'cond.c', 'sem.c' ])

Export("gyros_srcs")

SConscript(['target/SConscript'])
