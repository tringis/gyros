#!/bin/sh

if [ $# -ne 1 ]; then
    echo "Usage: $0 <target>" >&2
    exit 1
fi

TARGET="$1"

rm -f include/gyros/target
ln -s "$TARGET" include/gyros/target

rm -f src/target
ln -s "$TARGET" src/target
